
name: StopLight

on: 
    push:
        branches:
          - main
    pull_request:
        branches:
            - main

jobs: 
    StopLight:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v3
        
            - name: Using node.js
              uses: actions/setup-node@v2
              with: 
                node-version: 16.x

            - name: Install and Build
              run: |
                npm install

            - name: Stoplight
              run: |
                npx @stoplight/cli@6 push --ci-token 8011618f-ff7e-465a-a0bb-32a52fb81051 --url https://bruno-dela-val.stoplight.io/


